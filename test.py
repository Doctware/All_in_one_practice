#!/usr/bin/python3
def list_to_string():
    user_name_list = ['yusuf', 'sky', 'muideen', 'brother']
    sum_variable = ""
    for i in user_name_list:
        sum_variable = sum_variable + i + ", "
    print(sum_variable)

list_to_string()
